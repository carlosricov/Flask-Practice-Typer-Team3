from distutils.core import setup
from setuptools import find_packages

setup(
    name = "PracticeTyper",
    version = "0.9.0",
    description = "A Flask application dedicated to improving your programming typing speed."
)